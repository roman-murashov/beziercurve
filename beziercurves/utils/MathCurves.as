package beziercurves.utils {	import flash.geom.Point;			/**	 * @author nicolevav	 */	public class MathCurves {						public static function fact(n:int):Number {			if( n == 0 ) return 1;			return n * fact(n-1);		}				public static function binomialCoeff(n:int, k:int):Number {			return fact(n) / (fact(k) * fact(n - k));		}				public static function addPoints(p0:Point, p1:Point):Point {			return new Point(p0.x + p1.x, p0.y + p1.y);		}				public static function getNBezierStep(i:int, t:Number, n:int, p:Point):Point {			var px:Number = MathCurves.binomialCoeff(n - 1, i) * p.x * Math.pow(1 - t, n - 1 - i) * Math.pow(t, i);			var py:Number = MathCurves.binomialCoeff(n - 1, i) * p.y * Math.pow(1 - t, n - 1 - i) * Math.pow(t, i);			return new Point(px, py);		}	}}